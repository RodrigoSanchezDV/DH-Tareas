<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1 class="titulo">Película Editada:  <%= pelis.title %></h1>
    <section class="formulario">
        <form action="/movies/update/<%= pelis.id %>" method="POST">
            <label for="title">Title </label>
            <p><input type="text" name="title" id="" value="<%= pelis.title %>" required></p>
            <label for="rating">Rating </label>
            <p><input type="number" name="rating" id="" value="<%= pelis.rating %>" required></p>
            <label for="awards">Awards </label>
            <p><input type="number" name="awards" id="" value="<%= pelis.awards %>" required></p>
            <label for="release_date">Release date </label>
            <p><input type="date" name="release_date" id="" value="<%= pelis.release_date %> required"></p>
            <label for="length">Length </label>
            <p><input type="number" name="length" id="" value="<%= pelis.length %>" required></p>
            <label for="genre_id">Genre </label>
            <p>
                <select name="genre_id" id="" required>
                    <option value="">Seleccionar genero</option>
                    <% generos.forEach(genre => { %>
                    <option value="<%= genre.id %>" <%= pelis.genre_id == genre.id ? "Selected" : ""  %> ><%= genre.name %></option>
                    <% }); %>
                </select>
            </p>

    <!--
            <label for="genre_id">Genre ID </label>
            <p><input type="number" name="genre_id" id="" value=""></p>
        -->
            
            <br>
            <p> <button class="botonAgregar" type="submit">Actualizar</button>
        
            <a  class="botonVolver" href="/movies">Listado de Películas</a></p>
        </form>
    </section>
</body>

</html>